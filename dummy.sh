#دریافت ورودی
num=$1

# برسی میزان 512 موجود
count=$((num/512 | bc))

#  برسی مقدار باقی مانده
temp=$((num - count * 512))

# اگر صفر بود یعنی این عدده رند هستش
if ((temp == 0)); then 
	echo $temp
	exit
# در غیر این صورت باقی مانده رو حساب کن
else
	echo $((512 - temp))
fi 
